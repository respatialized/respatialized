image: jackfirth/racket

cache:
  paths:
    - .racket/

before_script:
  - raco pkg install pollen

pages:
  stage: deploy
  script:
    - make publish
  artifacts:
    paths:
      - public
  only:
    refs:
      - public
