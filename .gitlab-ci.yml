image: clojure:openjdk-14-boot-alpine

before_script:
  - "apk update && apk add rsync"

pages:
  stage: deploy
  script:
    - boot build
    - rsync -r ./target/ ./public
  cache:
    paths:
      - public
  artifacts:
    paths:
      - public
  only:
    refs:
      - perun
      - master
