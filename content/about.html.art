<%

(require '[clojure.java.classpath :as cp]
         '[clojure.string :as str]
         '[respatialized.render :as render])

%>

<%= (render/doc-header "about") %>
<%= (render/header "about") %>

This site has been made by adapting the <%= (str "https://github.com/hashobject/perun/tree/master/example-blog" "Perun example blog,") %> extended to the use of <%= (str "Ash-Ra-Templates") %>.
